;;; .doom.d/config.el -*- lexical-binding: t; -*-


;; Place your private configuration here

;; Package definitions

;; emacs M-arrows shortcut
;;; (after! org
;;;   (map! :map org-mode-map
;;;         :n "M-j" #'org-metadown
;;;         :n "M-k" #'org-metaup)
;;; )
